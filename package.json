{
  "name": "parcel-boilerplate",
  "description": "boilerplate parcel, gsap, splittingjs",
  "scripts": {
    "start": "parcel src/index.html --open",
    "clean": "rm -rf dist/*",
    "build:parcel": "parcel build src/index.html --no-content-hash --no-source-map --publi-url ./",
    "build": "npm run clean && npm run build:parcel"
  },
  "devDependencies": {
    "buffer": "^6.0.3",
    "parcel": "^2.5.0",
    "process": "^0.11.10"
  },
  "dependencies": {
    "@splinetool/react-spline": "^2.2.1",
    "@splinetool/runtime": "^0.9.52",
    "gsap": "^3.10.4",
    "react": "^18.1.0",
    "react-dom": "^18.1.0",
    "react-router-dom": "6",
    "splitting": "^1.0.6"
  }
}
